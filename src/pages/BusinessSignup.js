import styled from "styled-components";

const BSignup = () => {
  return <div></div>;
};

export default BSignup;

/* 
    Name
    Categories
    Location
    Services (including Photos)
    Description
*/

const Input = styled.input`
  padding: .5rem;

  font-family: 'poppins', sans-serif;
  font-size: 1rem;
  font-weight: normal;
  color: black;
`;

const 
